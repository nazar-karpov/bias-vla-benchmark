# ============================================================
#  BiasVLA — только КОД. Данные/меши/медиа/веса — не в репо.
# ============================================================

# ----- Чистые клоны upstream (клонировать отдельно, см. README) -----
/SimplerEnv/
/RL4VLA/

# ----- Вложенные .git апстримов (файлы храним, историю — нет) -----
.git/
!/.git/

# ----- 3D-меши и ассеты (данные, НЕ код) -----
*.glb
*.obj
*.ply
*.stl
*.dae
*.usd
*.usda
*.mtl
*.hdr
*.exr

# ----- Изображения / медиа -----
*.png
*.jpg
*.jpeg
*.gif
*.bmp
*.tiff
*.mp4
*.avi
*.mov
*.webm

# ----- Датасеты / выгрузки (данные, НЕ код) -----
*.csv
*.parquet
*.tfrecord*
*.jsonl
imgs/
images/
raw/
outputs/
datasets/

# ----- ML-веса / крупные бинарники -----
*.ckpt
*.pt
*.pth
*.safetensors
*.h5
*.npz
*.npy
*.bin
*.hdf5
*.pkl
*.pickle

# ----- Логи -----
*.log

# ----- Бэкапы -----
*.bak
*.bak_*

# ----- Python мусор -----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
# generic env/ выше глушит venv, но simpler_env/env/ — это КОД (SimplerWrapper),
# от которого зависит весь eval. Разгораживаем.
!Act2Answer/SimplerEnv/simpler_env/env/
!Act2Answer/SimplerEnv/simpler_env/env/**
.ipynb_checkpoints/

# ----- ОС / IDE -----
.DS_Store
Thumbs.db
.vscode/
.idea/

# ----- Секреты -----
.env
*.key

# спрятанные .git апстримов
*_HIDDEN_FOR_PUSH/
.git_HIDDEN_FOR_PUSH/

**/*_HIDDEN_FOR_PUSH/

# ----- Клоны upstream моделей (клонировать отдельно) -----
/InternVLA-M1/
/RLDX-1/
/internvla_ckpt/
/rldx_ckpt/
/internvla_base/

# ----- Логи прогонов -----
Act2Answer/logs/

# ----- Из feat: доп. правила -----
external/          # внешние клоны (REFLECT, SafeEditBench)
wandb/
runs/
results/**/raw/
# outputs_local/ НЕ игнорим целиком — там лежат результаты кроп-прогона (нужны в репе).
# Тяжёлое внутри (mp4/png/csv) и так покрыто правилами выше.

# Локальный чекаут стороннего репозитория X-VLA (2toinf/X-VLA)
X-VLA/
